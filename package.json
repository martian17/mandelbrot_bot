{
    "name": "mandelbrot",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "postinstall": "rm -rf ./build/ && node-gyp configure && node-gyp build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "canvas": "^2.8.0",
        "discord.js": "^12.5.3",
        "dotenv": "^10.0.0",
        "node-gyp": "^8.1.0"
    }
}